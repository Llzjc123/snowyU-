import {
  UserModel
} from '../../../models/user';

const app = getApp();
const userModel = new UserModel();

Page({
  data: {
  },

  onReady: function () {
  },

  onLoad: function (options) {
  },

  onShow() {
  }

})